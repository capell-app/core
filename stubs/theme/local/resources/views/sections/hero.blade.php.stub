<section class="{{ themeKey }}-section {{ themeKey }}-hero">
    @isset($eyebrow)
        <p>{{ $eyebrow }}</p>
    @endisset

    <h1>{{ $heading ?? $title ?? config('app.name') }}</h1>

    @isset($body)
        <div>{{ $body }}</div>
    @endisset
</section>
